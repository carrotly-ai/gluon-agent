# Audit Remediation — Progress (STATE.md)

Living tracker for implementing `docs/audit/gluon-simplicity-audit-2026-06-20.md` §9 roadmap.
Stacked onto **PR #154** (branch `feat/loop-engineering`). One roadmap item per loop
iteration; every commit gated on `ruff + mypy + pytest` (web-ui also `biome + build`).
Behavior-preserving except the two named latent-bug fixes.

## Gate baseline (known pre-existing noise — do NOT fix, do NOT add to)
- 3 mypy errors: `formulas.py`/`cli.py:2892` (yaml stubs), `agent_hooks.py:367` (arg-type).
- 1 environmental test failure: `tests/test_doctor.py::test_all_ok_for_clean_store` (host session disk usage).
Green = no NEW ruff/mypy errors in touched files, no NEW test failures.

## IN SCOPE (implement autonomously)

### Tier 1 — quick wins
- [x] **1. Latent bugs** `[bug/high/low]` — fix `formula run` (`anyio.from_thread.run`→`anyio.run`); wire `WorkQueueManager` finalization (`_finalize_queue_item`) so queue items stop leaking in RUNNING. +4 regression tests. *(commit: see below)*
- [x] **2. Debug prints** `[low/trivial/none]` — removed 9×`[RECOVERY]` (`api.py` `_run_recovery`) + 1×`[AGENT]` (`agent.py`) + a redundant `traceback.print_exc()`; all sat beside `logger.*` calls so logging behavior is unchanged.
- [x] **3. Leaf dead code (Clusters E–J)** `[med/low/very-low]` — re-grep each symbol before deletion; delete test-only symbols + their tests together. Done across 3A/3B-i/3B-ii/3C.
  - [x] **3A — prod-only zero-caller deletes**: websocket `stream_log_line`/`broadcast_todos_updated`; `agent.execute_simple`; resume_coordinator singleton (`_coordinator`+get/set/start/stop); `transport.get_telegram_transport`; capabilities `SLACK_CAPS`/`CLI_CAPS`; `models.Project.is_workspace_managed` + `ImageAttachment.to_markdown`; `commands.search_commands`; `run_telegram_transport`/`run_discord_transport`; runner `supervisor`/`supervisor_running` props. Gate green (2320 passed).
  - [~] **3B — test-only symbols + their tests**:
    - [x] **3B-i**: image_storage `save_image_from_file`/`copy_to_worktree`/`get_markdown_references` (+ TestCopyToWorktree/TestMarkdownReferences/test_save_from_file); `blueprint.run_validation` (+ TestRunValidation). −17 tests; suite 2303 passed.
    - [x] **3B-ii**: `policies.should_auto_resume` (+ TestShouldAutoResume); `recurrence.cron_to_recurrence` (+ orphaned `_CRON_TO_ISO` + 4 scattered tests, kept recurrence_to_cron tests); `bot_core.resolve_project` (+ TestResolveProject). **KEPT `bot_core.get_task`** — it's a legitimate symmetric accessor (register/unregister/get) and the only clean way TestTaskRegistration verifies the live register/unregister; removing it would force tests to inspect private `_active_tasks`. −11 tests; suite 2292 passed.
  - [x] **3C — misc + needs-care** (removed): store module-level `SCHEMA` constant (dead; `_init_db` uses inline DDL); `circuit_breaker.last_output_length` (write-only field; kept the `output_length` param for signature compat); transport capability boolean fields (6 unused `supports_*` + TELEGRAM/DISCORD_CAPS updated); `scheduler.TaskStatus` re-export (no importers); `events/types` 9 EXECUTION/SYSTEM event-name constants (0-ref; kept lifecycle/interaction). **KEPT (judgment):** `MODEL_IDS`/`_ModelIDsProxy` (documented public API in `docs/LLM-PROVIDER.md`); `cli recover --wait` flag (public CLI surface). **DEFERRED:** RedisEventTransport publisher trio (live-event-bus-adjacent — needs careful analysis; note for a follow-up). **SKIPPED `STALLED_THRESHOLD`** (live at health_monitor.py:142 — misleading-value fix, not a deletion).
- [x] **4. web-ui dead exports** `[med/low/very-low]` — removed 17 dead `api.ts` functions (createProject/fetchProject/updatePrStatus/fetchStatus/clearQueue/fetchFormula/validateFormula/fetchSchedule/fetchScheduleRuns/fetchMyLinks/fetchProjectGitStatus/refreshProjectGitStatus + image cluster uploadImage/fetchImage/deleteImage/attachImageToRun/detachImageFromRun), 12 orphan `types.ts` interfaces, `POLL_FAST`, and 4 now-unused type imports. **DEFERRED (#157):** the 13 advanced-git api.ts clients + their types. **SKIPPED (trivial polish):** `useCurrentUser` export-drop, `format.ts` stale TODO (item-15-adjacent), `resumeSdkSession` (known-broken visible button — note only). Gate: biome clean + `bun run build` ✓.
- [x] **5. store `in keys` guards** `[low/low/low]` — dropped ~50 obsolete `"col" in keys` guards in `_row_to_run` (columns always present post-migration). Pattern A (`else None`) collapsed to `row["col"]`; Pattern B's genuine `is not None`/truthiness NULL-handling preserved verbatim. Removed unused `keys = row.keys()`. Behavior-identical; suite 2292 passed. *(Same pattern remains in 3 smaller converters — workspace-budget/supervision-decision/one other; left as optional follow-up, audit named `_row_to_run`.)*

### Tier 2 — medium dedup
- [x] **6. web/api lookup helpers** `[high/med/low]` — added `_resolve_run_or_404` (folded 31 identical short-id-aware run-or-404 blocks), `_resolve_project_or_404` (folded 16 identical project-or-404 blocks), `_workspace_to_response` (folded 3 of 4 sites; left the deferred `/budget` route inline). **SKIPPED `_broadcast_run`** — the ~29 sites have DIVERGENT fallbacks (`run.project_id[:8]` vs `"Unknown"`) so it's not a clean behavior-identical fold. 1 run-or-404 variant (plain `get_run`, no short-id) left inline. Behavior-identical (api tests assert status+body); suite 2292 passed.
- [⊘] **7. runner `_run_task` seams** `[high/med/med]` — **DEFERRED to [#161](https://github.com/carrotly-ai/gluon-agent/issues/161)** after grounding. The audit's "duplicated VERBATIM" premise is false: the `_run_task` vs `_run_ralph_loop` finalization blocks have **drifted** (run/updated_run, persistent stdout/stderr handles vs re-opened `f`, different git-field capture, `item.success` gate vs none, and the ralph copy carries the item-A draft-PR/`gate_not_passed` logic). A unified `_finalize_worktree` is a multi-flag design call that changes warning-routing + PR behavior on the critical completion path — not safe to auto-merge (guardrail: don't force-merge divergent blocks). No code change.
- [x] **8. chat_agent tool cache** `[med/med/low]` — `chat()` rebuilt all 41 tool closures + the MCP server on every message; now built once and cached on `self` via `_get_mcp_server()` (closures capture only `self.orchestrator`, stable → safe). `allowed_tools` MCP portion derived from the registered `@tool` names (verified identical to the old hand-synced 41-entry list → behavior-identical; can no longer drift). Suite 2292 passed; direct check confirms 41 names + server reuse.
- [x] **9. git_manager `_run_gh`** `[med/med/low]` — added `_run_gh()` mirroring `_run_git`; folded the `pr view` site (returns None on failure, no log → provably identical); simplified the 4 redundant `except (FileNotFoundError, Exception)` → `except Exception` (FileNotFoundError ⊂ Exception). **Left the 3 logging gh sites inline** (get_pr_comments/get_check_runs/post_pr_comment) — folding them would change the warning-log message/emission on the (practically-unreachable) `gh`-missing path; return values would be identical but logs differ, so kept inline for strict behavior-preservation. The merge-base/numstat dedup (§4.7 tail) left as optional follow-up. Suite 2292 passed.
- [x] **10. budget enforcement** `[med/low/low]` — extracted `gluon/budgets.py` (`enforce_agent_budget`/`enforce_workspace_budget`/`touch_agent_last_active` + `_month_start_utc`); core.py methods + runner.py now both delegate (runner via aliased import → call sites unchanged). The pairs were behavior-identical (core inlined `month_start`/combined null-checks; verified equal to runner's `_month_start_utc`). `_check_safety_guards` did NOT exist as a def (audit approximation) — nothing to fold there. ~80 LOC dedup; no circular import (exceptions imported lazily); mypy now checks the moved code (was in mypy-ignored runner.py) — clean. Suite 2292 passed.
- [x] **11. dead formula routes** `[med/low/med]` — added `tests/test_api_formulas.py` (locks the LIVE list + run routes: list→200/list, run→404, run→success-mocked) FIRST, then removed the 2 internally-dead routes `GET /api/formulas/{name}` + `POST /api/formulas/validate` (0 callers; api.ts clients already gone in item 4). Live `GET /api/formulas` + `POST /api/formulas/{name}/run` kept. provider/budget/supervision untouched (#159). Suite 2295 passed (+3).

### Tier 3 — larger behavior-preserving refactors (each its own iteration, full-suite gated)
- [⊘] **12. web/api.py decomposition** `[high/med/low]` — **DEFERRED to [#162](https://github.com/carrotly-ai/gluon-agent/issues/162)** after grounding. The startup/shutdown hooks are deeply entangled (`nonlocal` task handles + `app.state` + 7 subsystems) and can't cleanly move; the 6 poll coroutines are untested `while True` loops on the live-update path (gate can mypy-check names but not verify behavior); the ~120-route APIRouter split needs a `Depends`/shared-context redesign (multiple valid approaches). Recommended human-led path: FastAPI lifespan + `AppContext` dataclass injected via `Depends`, then extract routers incrementally. No code change.
- [⊘] **13. PollingService base + Telegram QuestionWatcher** `[high/large/med]` — **DEFERRED to [#163](https://github.com/carrotly-ai/gluon-agent/issues/163)** after grounding. Part A (the real value): Telegram has NO `post_question_request` at all (only Discord implements the QuestionPoster) — so it's a feature implementation (formatting + inline keyboard + answer-callback + run-resume) with a routing design decision and an interactive flow that can't be gate-verified without a live Telegram bot; getting answer-recording wrong is worse than the status quo. Part B (dedup): `UndeliveredItemWatcher` generic is fiddly to keep byte-identical (logs/defaults) for ~50 LOC; the 6-engine `PollingService` base has divergent `tick` semantics. No code change.
- [⊘] **14. Unify auto-resume** `[high/large/med]` — **DEFERRED to [#164](https://github.com/carrotly-ai/gluon-agent/issues/164)** after grounding. Not actually a cap *bypass*: the two auto-resume counters (`supervision_auto_resume_count` cap 5, `auto_resume_count` cap 5) are SEPARATE triggers each respecting its own cap; the third path (`_handle_queued_followup`) is user-queued input, correctly uncapped. Only the *sum* can reach ~10. Merging the budgets into one cap changes legitimate cross-trigger behavior → a product decision (recommended: a separate overall safety/cost ceiling, value chosen by product, then tested). No code change.
- [⊘] **15. Merge RunDetail twins** `[high/large/med]` — **DEFERRED to [#165](https://github.com/carrotly-ai/gluon-agent/issues/165)**. The two components structurally diverged (Dialog 65 hooks superset vs Page 48); the ~20 shared handlers close over divergent local state (design call), and web-ui has NO component tests (gate is biome+build only) so an extraction can't be behaviorally verified. Recommended: add vitest + a `useRunActions` hook, refactor incrementally. No code change.
- [⊘] **16. Declarative store table-spec** `[med/large/med]` — **DEFERRED to [#166](https://github.com/carrotly-ai/gluon-agent/issues/166)**. 83 `_row_to_*`/`create_*`/`update_*` methods; a generic spec touches the central INSERT/SELECT/migration contract for every entity (highest blast radius) and must express JSON/enum/default columns — a design task. A one-entity POC doesn't de-risk the hard entities (ExecutionRun 60+ cols). Recommended: prototype on ExecutionRun behind the full suite. No code change. (Item 5 already removed the obsolete `in keys` guards, easing the readability pain.)

### Tier 4 — doc drift
- [x] **17. Doc fixes** `[low/low/none]` — chat_agent `run_task`/`resume_session` default model `sonnet`→`DEFAULT_MODEL.value` (opus-4.8) + SYSTEM_PROMPT model guidance (no longer claims sonnet is the default; notes Haiku not on Bedrock); Discord `models` help `(default)` moved to opus-4.8 + Haiku-not-on-Bedrock note. Suite 2295 passed (default change breaks no test). **Minor remaining (noted, low-value):** redis_transport.publish docstring (on a deferred test-only method), CLI-REFERENCE.md omissions, README endpoint/tool counts — pure prose, left for a docs pass.

## Deferred — tracked as issues (owner sign-off; reverses documented KEEP or deletes public API)
- [#155](https://github.com/carrotly-ai/gluon-agent/issues/155) — distributed Worker/Job/RedisJobQueue subsystem (~1,500 LOC) — remove or gate (§3A; KEEP at remediation-plan L220)
- [#156](https://github.com/carrotly-ai/gluon-agent/issues/156) — MergeQueueService dead at runtime — wire or remove (§5; KEEP at remediation-plan L217)
- [#157](https://github.com/carrotly-ai/gluon-agent/issues/157) — advanced-git UI unbuilt — build or delete 13 api.ts clients + GitManager methods (§5/§7)
- [#158](https://github.com/carrotly-ai/gluon-agent/issues/158) — witness recovery half — wire `execute_action` or delete (§3B)
- [#159](https://github.com/carrotly-ai/gluon-agent/issues/159) — externally-documented dead routes (provider/budget/supervision) — retire-or-keep (§3J)
- [#160](https://github.com/carrotly-ai/gluon-agent/issues/160) — §10 frontend-coupled "dead" fields — coordinated FE+BE change (RalphStatusBlock, ResumeRunResponse ids, auth OIDC branch)

## NEVER TOUCH (audit refuted — §10)
Do not merge `cleanup.py`↔`session_cleanup.py`; do not merge `image_storage.py`↔`files.py`;
do not delete `compute_next_fire` (ValueError contract); do not remove `GLUON_REDIS_URL`/`redis`
(feed the LIVE event bus).

## PHASE 2 — owner decisions received (2026-06-20); deferred items now GREENLIT
Items 1–17 done (11 shipped, 6 were deferred). Owner answered via AskUserQuestion — all unblocked:
- **Remove** (reverses documented KEEP): distributed queue #155, MergeQueueService #156, advanced-git clients #157, witness-recovery half #158.
- **Auto-resume #164:** add a combined hard ceiling = **8** (both auto paths; user follow-ups stay uncapped) + test.
- **Telegram QuestionWatcher #163:** build it, route to the approval chat (like Telegram approvals) + wiring test.
- **Refactors:** runner seams #161, web/api.py router split (lifespan+AppContext) #162, RunDetail merge (+vitest) #165, store table-spec (ExecutionRun first) #166.

### NEXT STEPS (Phase-2 queue, safest first)
- [x] **#155 distributed queue** — removed `queue/redis_queue.py` + pkg, the 17 worker/job store CRUD methods + `workers`/`jobs` DDL, the `Worker/Job/WorkerType/WorkerStatus/JobStatus` models, `tests/test_distributed.py`, and the Worker/Job cases in `test_models_domain.py`. Kept the LIVE event-bus redis transport. ~1,500 LOC; suite 2235 passed.
- [x] **#156 MergeQueueService** — deleted `merge_queue.py` (only contained the dead service, instantiated solely by its test) + `tests/test_merge_queue.py`. Kept the LIVE `merge_queue` store table/CRUD + read-only `/api/merge-queue` + `MergeQueuePage.tsx`. Suite 2229 passed.
- [x] **#157 advanced-git** — part 1 (f1972af): 13 dead api.ts clients + 8 orphan types. part 2 (backend): **GROUNDING CORRECTED the audit** — 8 of 13 GitManager methods are LIVE via chat_agent MCP tools (detect_conflicts/get_conflict_diff/resolve_conflict/rebase_continue/rebase_abort/list_branches/delete_branch), NOT dead. Removed only the 5 truly-api-only-dead methods (rebase_skip/force_push/check_force_push_needed/rename_branch/change_base_branch) + their 5 FastAPI routes (no api-test caller) + the 5 orphan models (ForcePushRequest/RenameBranchRequest/ChangeBaseBranchRequest/ForcePushCheckResponse/ForcePushResponse) + the rename_branch security test. Kept all chat_agent-used methods + BranchOperationResponse (shared). Suite 2228 passed.
- [x] **#158 witness recovery half** — removed `execute_action` + `_send_nudge`/`_recent_nudge_exists`/`_record_nudge_outcome` + `LOOPING_NUDGE_PROMPT`/`NUDGE_COOLDOWN_SECS` (0 prod callers) + the 5 `test_execute_action_*` tests. Kept `classify`/`suggest_action`/`_read_recent_output`/`_invoke_haiku` + the `RecoveryAction` enum (incl ESCALATE — a stored model value; suggest_action never emits it but keeping the member avoids deserialization risk). Suite 2223 passed.

**P1 removals complete (#155-158).** Next — the two scoped fixes:
- [x] **#164 auto-resume ceiling = 8** — added `MAX_TOTAL_AUTO_RESUMES = 8` to `models.py`; both auto paths refuse BEFORE resuming once `auto_resume_count + supervision_auto_resume_count >= 8`. PR-monitor: new guard in `should_monitor_run` (the single gate fronting both `auto_resume_for_comment`/`auto_resume_for_ci_failure`). Supervisor: early-return at the top of `_execute_resume` (single chokepoint after the policy decision). Per-trigger caps (`MAX_AUTO_RESUMES=5`, `SupervisionConfig.max_auto_resumes=5`) left intact — this ADDS a combined ceiling, doesn't replace them. `_handle_queued_followup` (user-queued) stays uncapped. New `tests/test_auto_resume_ceiling.py` (5 tests): each path blocks AT the ceiling even below its per-trigger cap, allows just below it, and the per-trigger cap still fires independently. Suite 2228 passed.
- [x] **#163 Telegram QuestionWatcher** — Telegram previously never surfaced `AskUserQuestion` (only Discord wired a `QuestionWatcher`), so questions from a Telegram-originated run silently stalled until timeout. Added to `transport/telegram.py`: `format_question_message` + `build_question_keyboard` (one button per option, callback_data `question:<id>:<idx>` < 64B), `post_question_request` (mirrors `post_approval_request` — posts to `approval_chat_id`, markdown→plain fallback, True-and-skip with no chat configured), `_handle_question_callback` (records the answer via the EXACT Discord/web path: `get_pending_question` → status guard → `question.answer([label], "user")` → `update_pending_question`; the runner picks it up on its next poll — no transport-side resume call), registered a `CallbackQueryHandler(pattern="^question:")`, and constructed/started/stopped a `QuestionWatcher` in `start()`/`stop()` alongside the approval watcher. New `tests/test_telegram_questions.py` (15 tests): formatter, keyboard, post (no-app/no-chat/happy/fallback), watcher tick via the Telegram poster, all callback paths (authorized records, unauthorized, already-answered no-overwrite, malformed, missing, out-of-range), and `start()` wires a running `QuestionWatcher`. Suite 2243 passed. **PR NOTE:** the live button→answer→resume round-trip needs manual Telegram verification (single-press records one option; multi_select records a single choice — Telegram inline-button limitation, noted in the formatter message).
Then P3 refactors #161 → #162 → #165 → #166.
P2 fixes: #164 ceiling=8 (+test) → #163 Telegram question flow (+test).
P3 refactors: #161 → #162 → #165 (vitest first) → #166.

**P3 progress:**
- [⊘] **#161 runner `_run_task` seams — RE-ATTEMPTED in Phase 2 (owner greenlit "attempt all 4"), DEFER CONFIRMED.** Re-grounded `_run_task` (981–1908) vs `_run_ralph_loop` (2656–2883). The candidate "shared finalization tail" does NOT exist as a clean seam — the two PR/git-ops blocks (`_run_task` 1455–1549 vs `_run_ralph_loop` 2767–2862) diverge on **7 axes**: (1) output writer — persistent `stdout_file.write()+flush()` vs reopen-`with open(stdout_path,"a")`-per-write; (2) git_info capture — task sets branch+sha+pr_number+pr_url+pr_status, ralph sets branch+sha only with `or`-fallback; (3) `gate_not_passed`/draft-PR + `_mark_pr_gate_not_passed` is **ralph-only**; (4) guard `... and item.success` is **task-only**; (5) `store.update_run` called inline in ralph, not task; (6) commit label "Gluon Agent" vs "Gluon Agent (Ralph Loop)"; (7) **decisive structural difference** — in `_run_task` the block runs **mid-method, BEFORE blueprint re-validation** (line 1551+, which re-invokes the agent and can add commits/snapshots), whereas in `_run_ralph_loop` it is the actual tail. Unifying would relocate task's PR logic relative to its blueprint step = a behavior change on the critical completion path of the repo's highest-risk, mypy-ignored core engine (live PR paths aren't exercised by tests). A `_finalize_worktree(run, write_out, write_err, commit_label, capture_pr, gate_not_passed, ...)` would be a 7-flag conditional maze — riskier + less readable than the current duplication. Per the queue guardrail ("diverged beyond safe parameterization → DEFER"), left as-is; issue [#161](https://github.com/carrotly-ai/gluon-agent/issues/161) updated with this grounding. No code change.
- [x] **#162 web/api.py — SAFEST SLICE SHIPPED (6 background coroutines → `web/background.py`).** Grounded all 6 (`_poll_run_status_changes` / `_poll_log_updates` / `_poll_pr_status_changes` / `_cleanup_old_logs` / `_cleanup_old_worktrees` / `_sweep_auth_state`): each closes over a **small, explicit** dep set (store / runner / get_project_lookup / the 4 mutable tracking dicts / 2 cleanup constants) — NOT "dozens of locals" — so the extract-criterion held (unlike #161). Moved them **byte-identical** to `gluon/web/background.py` as module-level coroutines taking those deps as params (generated by programmatically slicing the exact bytes + renaming only the 4 `_`-prefixed tracking dicts to clean param names + rewriting the 6 signatures — zero hand-transcription). `ws_manager` (a module-level singleton) is imported directly rather than threaded through. The startup hook now `create_task(background.<fn>(store, runner, …))`; the 4 tracking dicts + 2 cleanup constants stay defined in `create_app` and are passed in. api.py 6625→6263 LOC (−362). Added `gluon.web.background` to the mypy ignore override (consistent with the already-ignored `gluon.web.api` it came from). New `tests/test_web_background.py` (5): signature-lock for all 6 + behavioral run-as-task-then-cancel smoke tests for the 3 immediate-tick pollers (broadcast-on-state-change, no-broadcast-for-new-run, log-poll no-op, PR-poll no-op) — a regression net the in-closure loops never had (startup lifespan isn't triggered by the suite). Suite 2248 passed. **STILL DEFERRED (the design-call part of #162):** the ~120-route APIRouter split + the lifespan/`AppContext`-via-`Depends` redesign + the entangled startup/shutdown hooks (7 subsystems, `nonlocal` task handles) — those remain in issue [#162](https://github.com/carrotly-ai/gluon-agent/issues/162) as a human-led design decision.
- [x] **#165 RunDetail merge — HARNESS SHIPPED, merge DEFERRED.** STEP 1 (the explicitly-greenlit, valuable half): web-ui had **no** test harness (gate was biome+build only). Added vitest 4 + @testing-library/react + jest-dom + jsdom, a standalone `vitest.config.ts` (jsdom, mirrors only the `@/` alias; omits the PWA/Tailwind plugins), `src/test/setup.ts` (jest-dom matchers + RTL cleanup), `test`/`test:watch` scripts, and excluded `*.test.*`/`src/test` from `tsconfig.app.json` so `tsc -b` (build) ignores them. Smoke coverage: `format.test.ts` + `smoke.test.tsx` (6 tests, green); `bun run build` unaffected. Commit `6f76604`. STEP 2 (the merge) — re-grounded `RunDetailDialog.tsx` (2892 LOC) vs `RunDetailPage.tsx` (2041 LOC): ~18 shared handler names but they **diverge** like #161 — the Page is a routed view that **owns its own `run` state** (`setRun`) and uses optional `onRunUpdated?.`, while the Dialog receives `run` as a prop with a **required** `onRunUpdated` and shows **toasts the Page doesn't** (e.g. `handleCancel`: Dialog toasts + `onRunUpdated`, Page `setRun` + `onRunUpdated?.` + no toast). A `useRunActions` hook must parameterize state-ownership + callback-optionality + toast-UX across all ~18 → a conditional-heavy surface whose behavior-identity can't be verified without comprehensive component tests that don't exist yet, on the primary run-detail UI. Per the task's explicit allowance + the #161 guardrail, DEFERRED the merge to issue [#165](https://github.com/carrotly-ai/gluon-agent/issues/165) — now **unblocked** for incremental, test-first handler extraction thanks to the harness.
- [⊘] **#166 store table-spec — RE-ATTEMPTED in Phase 2, DEFER CONFIRMED (no code change).** Grounded the `execution_runs` persistence: the base `CREATE TABLE` has only **13 columns**; the other ~67 are added via **76 `ALTER TABLE execution_runs ADD COLUMN`** entries in the append-only `MIGRATIONS` list (a NEVER-TOUCH). `_row_to_run` reads **89 columns** with 7+ distinct *bespoke* per-column transforms — 3 enum coercions (`RunStatus`/`CircuitState`/`ApprovalPolicy`, each with a different fallback), 13 `_parse_datetime`, 6 bool-from-int **with per-column defaults** (False vs True), int-with-varying-defaults (0/50/100), `Path()`-wrap, `json.loads`→dict, and `json.loads`→model-list. A declarative `(name, sqltype, default)` spec therefore **cannot own the schema** (it's 13-col base + 76 untouchable migrations → it would have to be hand-synced against the migration list, ADDING a burden), and to drive the round-trip it would have to encode all ~89 transforms+defaults into a `dict[str, Callable]` registry — **relocating** the complexity, not reducing it, while type-checking **worse** than the explicit `ExecutionRun(...)` constructor in this mypy-CHECKED, corruption-sensitive path (a single wrong serializer/deserializer/default silently corrupts run state). This is the audit's "abstraction longer + riskier than the duplication it replaces" trap (the #161/#165 pattern). Per the task ("if it fights the hand-rolled mapping / risks schema-migration drift → REVERT + DEFER; a clean no-op is fine"), DEFERRED to issue [#166](https://github.com/carrotly-ai/gluon-agent/issues/166). No code written (grounding was conclusive — speculative code would only have been reverted).
Each: re-grep/ground, behavior-preserving (fixes get tests), gate `ruff+mypy+pytest` (+biome+build for web-ui), commit to PR #154, update this tracker. GROUND+DIFF FIRST: `ResumeCoordinator` (`supervision_auto_resume_count` vs `config.max_auto_resumes`), `PRMonitorService` (`auto_resume_count` vs hardcoded `MAX_AUTO_RESUMES=5`), and the queue-drain path each call `runner.resume_in_place` with their own counter+cap → a run can exceed either nominal cap via both paths. If the 3 engines' resume semantics are reconcilable, route them through ONE `ResumeCoordinator.request_resume(run, trigger, prompt)` with a single counter+cap and add a test proving the combined cap holds. If their triggers/semantics differ subtly (likely — supervision vs PR-comment vs queue), this is a design call → DEFER with an issue. This is the one item with a real cap-bypass BUG, so even if the full unification defers, consider whether a minimal shared-cap fix is safely testable. GROUND+DIFF FIRST (item-7 lesson): the 6 "keep-running" engines (resume_coordinator/scheduler/task_scheduler/question_watcher/approval_watcher/health_monitor) each hand-roll `_running`/`_task`/`start`/`stop`/`is_running`/`_run_loop` — confirm they're structurally identical enough for a `PollingService` base before extracting; if they diverge, DEFER. The ApprovalWatcher/QuestionWatcher are ~95% copy-paste (differ only in 3 store/poster method names) → a generic `UndeliveredItemWatcher`. SEPARATELY, the real behavior fix: Telegram never wires a QuestionWatcher (only Discord does) → AskUserQuestion from Telegram silently never surfaces; wiring it needs a test. Consider splitting: (a) the safe base-class/watcher extraction, (b) the Telegram-watcher fix (with test). If the base extraction is too divergent, do just (b) and DEFER (a). GROUND FIRST: `create_app` is one ~6900-line closure capturing `store`/`runner`/`ws_manager`/`notifier`/`get_project_lookup`/`run_to_response`/the new `_resolve_*` helpers etc. Splitting ~120 routes into `web/routes/*` APIRouter modules requires threading that closure state through Depends or a shared context — a real design call. SAFEST SLICE first: extract the **6 background coroutines** (`_poll_run_status_changes`/`_poll_log_updates`/`_poll_pr_status_changes`/`_cleanup_old_logs`/`_cleanup_old_worktrees`/`_sweep_auth_state`) to `web/background.py` as functions taking explicit deps, called from the startup hook — IF cleanly separable. If even that entangles with closure state non-trivially, DEFER item 12 (issue + note) — do NOT big-bang a 6900-line file. Behavior-identical only; FULL suite.
